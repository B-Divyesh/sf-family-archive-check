[
  {
    "id": "demo-ready",
    "claim": "The sample opens as a finished two-folder check.",
    "where": "landing primary action and demo",
    "test": "npm run test:e2e -- --grep @claim:demo-ready",
    "sandbox": "fresh browser context at /demo; assert the named missing video and demo banner"
  },
  {
    "id": "manifest-export",
    "claim": "Exports a portable recovery manifest.",
    "where": "landing and result action",
    "test": "npm run test:e2e -- --grep @claim:manifest-export",
    "sandbox": "demo data; download JSON and assert the check id and missing path"
  },
  {
    "id": "handoff-sheet",
    "claim": "Opens a printable recovery handoff sheet.",
    "where": "result action and README",
    "test": "npm run test:e2e -- --grep @claim:handoff-sheet",
    "sandbox": "demo data; open the handoff route and assert its recovery steps, demo banner, and single main landmark"
  },
  {
    "id": "local-only",
    "claim": "Files stay on this device.",
    "where": "landing fact and privacy section",
    "test": "npm run test:e2e -- --grep @claim:local-only",
    "sandbox": "fresh browser context at /demo; record all requests through reset and export; assert same-origin only"
  },
  {
    "id": "offline-reload",
    "claim": "Works offline after the first visit.",
    "where": "landing fact",
    "test": "npm run test:e2e -- --grep @claim:offline-reload",
    "sandbox": "fresh browser context at /demo; wait for service worker, reload once, go offline, reload"
  },
  {
    "id": "compare-copies",
    "claim": "Reports missing, changed, extra, and unreadable entries by comparing paths, sizes, and sampled hashes.",
    "where": "how it works and desktop check",
    "test": "npm run test:unit -- --testNamePattern @claim:compare-copies",
    "sandbox": "in-memory main and copy scans with one missing and one changed media file"
  },
  {
    "id": "media-readable",
    "claim": "Sampled media must have a valid structure before it is reported readable.",
    "where": "check status and result evidence",
    "test": "npm run test:unit -- --testNamePattern @claim:media-readable",
    "sandbox": "in-memory empty and truncated JPEG and PNG bytes; assert each is rejected"
  },
  {
    "id": "independent-folders",
    "claim": "The same folder or storage device cannot pass as an independent copy.",
    "where": "folder check",
    "test": "npm run test:unit -- --testNamePattern @claim:independent-folders",
    "sandbox": "in-memory scans with matching paths or native storage identifiers; assert each is blocked"
  },
  {
    "id": "free-limit",
    "claim": "Free checks include up to 500 files.",
    "where": "landing price fact, check license panel, and README",
    "test": "npm run test:unit -- --testNamePattern @claim:free-limit",
    "sandbox": "in-memory boundary of 500 and 501 files with and without an active license"
  },
  {
    "id": "read-only",
    "claim": "Checks folders without changing archive files.",
    "where": "landing privacy section and README",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml claim_read_only",
    "sandbox": "temporary folder and media file; scan, then assert the file bytes are unchanged"
  },
  {
    "id": "capture-year",
    "claim": "The report measures how much main media has a known year from metadata or its path.",
    "where": "result evidence",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml claim_capture_year",
    "sandbox": "temporary dated folder containing a valid image; assert the scanner records its year"
  },
  {
    "id": "paid-license",
    "claim": "A $29 household license enables unlimited checks and saved profiles.",
    "where": "paid tier",
    "test": "npm run test:e2e -- --grep @claim:paid-license",
    "sandbox": "fresh browser context with a recorded valid verification response; assert checkout URL, exact price, and active benefits"
  },
  {
    "id": "platform-download",
    "claim": "Links the detected desktop platform to its release asset.",
    "where": "landing download section and README",
    "test": "npm run test:e2e -- --grep @claim:platform-download",
    "sandbox": "fresh browser context reporting macOS with a recorded GitHub release response; assert the DMG download URL"
  },
  {
    "id": "demo-isolation",
    "claim": "Demo data stays separate from real folder data.",
    "where": "demo banner, demo guide, and README",
    "test": "npm run test:e2e -- --grep @claim:demo-isolation",
    "sandbox": "fresh browser context; leave the sample through Check folders and assert the sample entry is absent"
  },
  {
    "id": "license-privacy",
    "claim": "A license check sends only the pasted token to Sociobot.",
    "where": "privacy page and README",
    "test": "npm run test:e2e -- --grep @claim:license-privacy",
    "sandbox": "recorded license response; assert one GET contains only the encoded token and no body"
  },
  {
    "id": "installer-checksum",
    "claim": "Each command-line helper verifies SHA-256 before saving an installer to Downloads.",
    "where": "README",
    "test": "npm run test:unit -- --testNamePattern @claim:installer-checksum",
    "sandbox": "inspect both shipped helpers; assert checksum verification precedes the move to Downloads"
  }
]
